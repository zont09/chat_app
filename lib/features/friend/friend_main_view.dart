import 'package:flutter/material.dart';

class FriendMainView extends StatelessWidget {
  const FriendMainView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Material(
      child: Center(
        child: Text('Friend Main View'),
      ),
    );
  }
}