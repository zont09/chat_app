import 'package:flutter/material.dart';

class GroupMainView extends StatelessWidget {
  const GroupMainView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Material(
      child: Center(
        child: Text('Group Main View'),
      ),
    );
  }
}